name = "taxist"
pages_build_output_dir = "."
compatibility_date = "2024-09-23"
compatibility_flags = ["nodejs_compat"]

# D1 데이터베이스 바인딩
# database_id는 `wrangler d1 create taxist-db` 실행 후 채워주세요
[[d1_databases]]
binding = "DB"
database_name = "taxist-db"
database_id = "REPLACE_WITH_YOUR_D1_ID"
