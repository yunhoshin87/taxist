-- ============================================================
-- v3 마이그레이션: 답변 자료 충족도 추적
-- ============================================================

-- answers.coverage_gap:
--   0 (기본) = 참고자료가 충분해 정상 답변 생성됨
--   1        = Gemini가 참고자료로 답변 불가 판정 → "자료부족" 경고 표시 대상
--              (주로 간단 질의와 채팅에서 발생; 일반 질의는 "추가 확인 필요사항" 섹션으로 처리)
ALTER TABLE answers ADD COLUMN coverage_gap INTEGER DEFAULT 0;
